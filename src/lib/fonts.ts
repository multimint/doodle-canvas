// The hand-drawn "doodle" font used for on-canvas Text Boxes (and mirrored by the --disp
// CSS variable for the editor textarea). Quoted family name + fallbacks so the same
// string works in CSS and in the Canvas 2D `context.font` shorthand Konva builds.
export const DOODLE_FONT = "'Patrick Hand', system-ui, sans-serif"
